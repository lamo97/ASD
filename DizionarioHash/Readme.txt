Implementazione della struttura Dizionario con Hash Table tramite vettore e liste, per la gestione dei conflitti.
La struttura LinkedList, a differenza di quella originale, contiene una piccola modifica per far si che, se la posizione data 
come parametro a readValue non è valida, viene restituito un valore vuoto non inizializzato, questo per evitare probemi con il compilatore o con i valori (dal
momento che NULL non era adatto).
